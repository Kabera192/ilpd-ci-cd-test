# =====================================================
# DEPLOYMENT - Defines pods and containers
# File: k8s/deployments/auth-service-deployment.yaml
# =====================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: hostel-service
  namespace: ilpd-mis
  labels:
    app: hostel-service
    version: v1
spec:
  replicas: 2  # Run 2 instances for high availability
  selector:
    matchLabels:
      app: hostel-service
  template:
    metadata:
      labels:
        app: hostel-service
        version: v1
    spec:
      containers:
      - name: hostel-service
        image: kaberagclap/hostel-service:fd3585f # Tag is the hash of the lates commit from the command: `git rev-parse --short HEAD`
        imagePullPolicy: Always
        ports:
        - containerPort: 8087
          name: http
        
        # Environment variables from ConfigMap
        envFrom:
        - configMapRef:
            name: hostel-service-configmap
        - configMapRef:
            name: common-configmap
        
        # Environment variables from Secret
        env:
        - name: DB_USERNAME
          valueFrom:
            secretKeyRef:
              name: common-secrets
              key: DB_USERNAME
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: common-secrets
              key: DB_PASSWORD
        - name: MONGO_USER
          valueFrom:
            secretKeyRef:
              name: common-secrets
              key: MONGO_USER
        - name: MONGO_PWD
          valueFrom:
            secretKeyRef:
              name: common-secrets
              key: MONGO_PWD
        - name: MONGO_AUTH_DB
          valueFrom:
            secretKeyRef:
              name: common-secrets
              key: MONGO_AUTH_DB
        
        # Liveness probe - restart if unhealthy
        livenessProbe:
          httpGet:
            path: /actuator/health/liveness
            port: 8087
          initialDelaySeconds: 60
          periodSeconds: 10
          timeoutSeconds: 3
          failureThreshold: 3
        
        # Readiness probe - remove from load balancer if not ready
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8087
          initialDelaySeconds: 30
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
      
      # Graceful shutdown
      terminationGracePeriodSeconds: 30
